
secret
# Added by cargo

/target

# Auto Claude data directory
.auto-claude/

# Local CI testing
.actrc
.secrets

# Debug capture artifacts
response.json

# Agent tooling scratch state
.auto-claude-*
.claude_settings.json

# Subagent worktrees
.claude/worktrees/

# Docker deployment: bind-mount host paths. `data/config/config.toml` holds the
# Canvas PAT (and sso_password in cleartext) once the user follows the compose
# quickstart, so it must never be committable.
data/
.env
